public class Token {

    private int message; // the tokens unique message

    // constructor of the token
    Token(int message)
    {
        this.message = message; // sets the message of the token
    }

    // getter for the message
    public int getMessage()
    {
        return message;
    }

}
